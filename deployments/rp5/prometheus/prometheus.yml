global:
  scrape_interval: 60s

alerting:
  alertmanagers:
    - static_configs:
        - targets:
            - alertmanager:9093

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: 'raspberry_pi'
    static_configs:
      - targets: ['node-exporter:9100']
